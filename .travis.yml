language: java
sudo: required
jdk:
  - oraclejdk8
install:
  - cd desapp-grupox-backend && mvn clean install -Dmaven.compiler.target=1.8 -Dmaven.compiler.source=1.8

notifications:
  email:
    recipients:
      - a.redonda89@gmail.com
      - gil.maricruz@gmail.com
    on_success: always # default: change
on_failure: always # default: always
